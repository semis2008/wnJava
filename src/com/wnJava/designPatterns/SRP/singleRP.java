package com.wnJava.designPatterns.SRP;

import com.wnJava.designPatterns.common.commonDP;

/**
 * 单一职责原则
 * @author kalor
 *
 */
public class singleRP extends commonDP{
	/*
	 * 构造函数
	 */
	singleRP() {
		setDp_name("单一职责原则_SingleResponsibilityPrinciple");
		setDp_info("There should never be more than one reason for a class to change._一个类需要进行改变的原因，必须只有一个。");
	}
}
